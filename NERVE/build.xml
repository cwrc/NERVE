<?xml version="1.0" encoding="UTF-8"?>
<!-- You may freely edit this file. See commented blocks below for -->
<!-- some examples of how to customize the build. -->
<!-- (If you delete it and reopen the project it will be recreated.) -->
<!-- By default, only the Clean and Build commands use this build script. -->
<!-- Commands such as Run, Debug, and Test only use this build script if -->
<!-- the Compile on Save feature is turned off for the project. -->
<!-- You can turn off the Compile on Save (or Deploy on Save) setting -->
<!-- in the project's Project Properties dialog box.-->
<project name="NERVE3" default="default" basedir=".">
    <description>Builds, tests, and runs the project NERVE3.</description>
    <import file="nbproject/build-impl.xml"/>

    <property name="keyfile" value="C:/cygwin64/home/edward/.ssh/id_rsa"/>
    <property name="remoteTarget" value="root@dh.sharcnet.ca:/www/webapps/"/>

    <target name="-pre-dist">
        <antcall target="copyLibraryFiles"></antcall>
    </target>

    <target name="copyLibraryFiles" description="Copy libs to WEB-INF/lib">
        <mkdir dir="war/WEB-INF/lib" />
        <copy todir="war/WEB-INF/lib">
            <fileset dir="../EncoderDecoder/dist/" includes="*.jar"/>
            <fileset dir="../EncoderDecoder/dist/lib" includes="*.jar"/>

            <fileset dir="../DocumentNavigator/dist/" includes="*.jar"/>
            <fileset dir="../DocumentNavigator/dist/lib" includes="*.jar"/>
        </copy>
    </target>

    <target name="deploy">
        <echo>copy to ${remoteTarget}</echo>
        <scp todir="${remoteTarget}" keyfile="${keyfile}" trust="yes">
            <fileset dir="./dist" includes="*.war"/>
        </scp>
    </target>

</project>
